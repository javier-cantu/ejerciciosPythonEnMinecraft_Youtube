# Ejercicio 007: Teletransportarse a varios lugares

#Conectarse a Minecraft
from mcpi.minecraft import Minecraft
mc = Minecraft.create()
#Necesitamos importar el modulo de tieme para tener acceso a funciones que usan tiempo
import time

#establecer las variables x y z para las coordenadas. 
x = -122
y = 5
z = 86

# Cambiar la posición del jugador
mc.player.setTilePos(x, y, z)
# Se manda mensaje a la consola
mc.postToChat("Posicion 1")

# Pausa por 5 segundos
time.sleep(5)

#Se le asignan nuevos valores a las variables x, y, z
x = 0
y = 5
z = 0

# Se cambia la posición del jugador de nuevo
mc.player.setTilePos(x, y, z)
mc.postToChat("Origen")
